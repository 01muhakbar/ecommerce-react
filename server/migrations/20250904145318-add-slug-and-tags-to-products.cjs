'use strict';
module.exports = {
  up: async (queryInterface, Sequelize) => {
    // This migration was empty. Adding a placeholder to allow migration to continue.
  },
  down: async (queryInterface, Sequelize) => {
    // This migration was empty. Nothing to do.
  }
};